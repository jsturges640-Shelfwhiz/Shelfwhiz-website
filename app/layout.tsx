import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "ShelfWhiz - Inventory Optimization Solution",
  description:
    "Maximize warehouse efficiency and minimize effort with ShelfWhiz. Accelerate inventory setups, changeovers, and line expansions with optimal SKU placement.",
  generator: "v0.app",
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
